# SCT_CS_2
A Python-based web tool (Flask + HTML/CSS/JS) for educational purposes, demonstrating image encryption and decryption using pixel manipulation. Supports operations like XOR and pixel swapping with a secret key to securely transform images at the pixel level.
